# 枚举

## enum CompileStage

```cangjie
public enum CompileStage {
    | BeforeMacro
    | AfterSema
    | ...
}
```

功能：表示编译阶段的类型。

### BeforeMacro

```cangjie
BeforeMacro
```

功能：表示宏展开之前的编译阶段。

### AfterSema

```cangjie
AfterSema
```

功能：表示语义分析之后的编译阶段。
